package rr;

import java.util.Arrays;

public class Array
{
	public static void main(String[] args) 
	{
		
		  /*int[] arr = {4,5,6,-8,0,-6,7,-3,0,9,-5};
		  
		  // Sort the array in ascending order Arrays.sort(arr);
		  
		  // Print the sorted array System.out.println("Array in ascending order:");
		  for (int i=0;i<=arr.length;i++) { System.out.print(arr[i] + " "); }
		 
		   */
		/*
		 * String r="rrrrrghaa"; String rev =""; for (int i=r.length()-1;i>=0;i--) {
		 * 
		 * if (r.charAt(i)!='r') { rev = rev+r.charAt(i); }
		 * 
		 * 
		 * } System.out.println(rev);
		 */
		
		/*String r="amma";
		
		String same ="";
		for(int i=r.length()-1;i>=0;i--)
		{
			same = same+r.charAt(i);
			if (r.equals(same)) 
			{
				System.out.println(same);
			}
		}*/
		
		String[] e={"ramaraju", "mani", "sawami"};
		for (int i = e.length-1; i >=0; i--)
		{
			System.out.println(e[i]+"");
		}
		
	}

	
}
